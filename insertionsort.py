"""
 Insertion sort algorithm:
  Marks the non sorted portion of the list. Picks its first element and
  compares it to the elements on the left (the sorted portion) and
  inserts the element where it belongs.
  In practice, a bit more efficient than bubble or selection sort
"""


def insertion_sort(lst):
    pass

